# Script Prompt

The official runner writes the exact selected-concept prompt with:

```bash
npm run format:talking-fish-news -- script-prompt --run <run-id>
```

The prompt locks four beats: breaking hook, what happened, why it matters, and the approved deadpan punchline. It requires 38-60 words, source-grounded facts, plain spoken language, and no sales pitch or invented claim.
